export default function EventsPage() {
  return (
    <main className="min-h-screen bg-midnight text-ivory pt-40 px-6">
      <div className="container mx-auto max-w-5xl">
        <p className="font-display text-gold tracking-[0.3em] uppercase text-xs mb-6">Event Curation</p>
        <h1 className="font-serif text-6xl md:text-8xl text-ivory mb-10 leading-[1]">
          Events &<br /><span className="italic text-gold">Photography.</span>
        </h1>
        <p className="font-sans font-light text-smoke text-lg leading-relaxed max-w-2xl">
          Our dedicated event curation team and in-house photography studio ensure
          every moment is captured with the grandeur it deserves.
        </p>
      </div>
    </main>
  );
}
